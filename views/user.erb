<div class="row">
	<% user.map { |u| %>

	<h1><%= "#{u.fname} #{u.lname}" %></h1>
	<div class="lg-12 columns">
		<h3><%= "#{u.username}" %></h3>
		<h3><%= "#{u.email}" %></h3>
	</div>
	<%  } %>
	

</div>